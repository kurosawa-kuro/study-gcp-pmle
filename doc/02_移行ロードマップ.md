# 移行ロードマップ

> **本リポジトリの決定的仕様**。何を作るか・何を作らないかの最終決定。
> スコープ確定・方針変更時に更新する。矛盾時はこのドキュメントを正とする。

## 0. 前提

- **成果物**: PMLE（Google Cloud Professional Machine Learning Engineer）合格直結の**判断辞書型教材**（日本語 Markdown）。アプリ・DB・インフラは持たない。
- **対象試験**: 現行 PMLE。出題範囲・比率は公式試験ガイドに追従する（数値の正は `01_仕様と設計.md §1`）。
- **設計の正**: コンセプト・8 項目テンプレート・比較表・シグナル語・出題比率は `01_仕様と設計.md` を正とする。本書は重複を避け、必要時はそこを参照する。
- **スコープ境界**: 教材コンテンツ本体（`src/` の章原稿 + `doc/` の仕様）のみ。学習プラットフォーム・採点アプリ・実装ハンズオン教材は作らない（§5）。

---

## 1. 流用資産マップ

| 資産 | 用途 | 流用可否 |
|---|---|---|
| `01_仕様と設計.md` | 8 項目テンプレ / 比較表 / シグナル語 / 出題比率の正本 | ◎ 全章がこれに従う |
| `doc/README.md` | 命名・節番号・裏取り・連動更新の運用規約 | ◎ 全ドキュメントが従う |
| `../study-gcp-search-mlops-gke/`（隣接プロジェクト） | GCP ML 実装（Composer / Feature Store / KServe / Vertex）の**裏取り補助** | △ 知識の裏取りに参照。コード転用はしない |

### 1.1 新規開発が必要（既存流用不可）

- `src/` 配下の全章原稿（既存教材なし。ゼロから執筆）
- 各章の代表問題パターン（自作シナリオ。実試験問題は転載不可）

---

## 2. 段階構築計画

> 各章は `01_仕様と設計.md §2.1` の 8 項目テンプレートで統一する（完了条件に含む）。
> 章 = サービス名ではなく**意思決定単位**で立てる。

### Phase 0: プロジェクト初期化
- 内容: テンプレ整備、`doc/01`〜`04` の骨格、`src/` 命名規則確定。
- 完了条件: ✅ ドキュメント骨格と運用規約（`doc/README.md`）が存在する。

### Phase 1: 序章 + 最重点ドメイン（出題比率上位）
- 内容: `src/00_試験の世界観.md` / `01_モデル構築方式の選択.md` / `02_カスタム学習を本番にスケールする.md` / `03_本番サービングの選択.md` / `04_MLパイプラインの自動化とオーケストレーション.md` / 付録A・B / `src/README.md`。
- 完了条件: ✅ 上記が 8 項目テンプレを満たし、各章に代表問題 2〜3 問・フローチャート・比較表があり、揮発情報に `⚠ 要公式確認` が付く。

### Phase 2: 運用・データ管理ドメイン
- 内容（章）:
  - `src/05_データと特徴量の管理.md`（Dataflow / Dataproc / BigQuery 前処理 / Feature Store / TFT / skew 防止）
  - `src/06_実験管理と再現性と系譜.md`（Experiments / ML Metadata / Model Registry / TensorBoard）
  - `src/07_モデル運用監視の判断.md`（Model Monitoring: training-serving skew / drift / attribution drift）
  - `src/08_責任あるAIと説明可能性と評価.md`（Explainable AI / Model Evaluation / fairness・bias / 機密保護）
- 完了条件: ✅ 各章が 8 項目テンプレを満たし、付録A（§A.6〜A.9）・付録B（§B.4・B.5・B.7・B.11）に当該シグナル語・比較軸が反映済み。

### Phase 3: 生成AI + 横断ドメイン
- 内容（章）:
  - `src/09_生成AI活用とRAGの判断.md`（Model Garden / Gemini API / Vertex AI Search / Embeddings + Vector Search / Grounding）
  - `src/10_生成AIチューニングと評価.md`（プロンプト設計 / Fine-tuning / 蒸留 / Gen AI 評価）
  - `src/11_データ所在地とコストとセキュリティ.md`（region / VPC-SC / CMEK / IAM・WIF / コスト最適化）
- 完了条件: ✅ 各章が 8 項目テンプレを満たし、付録A（§A.10〜A.12）・付録B（§B.8・B.12・B.13）に反映済み。
  - ⚠ **未了**: 生成AIサービス名の「現行公式名での確定（`⚠ 要公式確認` 解消）」は web 裏取り未実施のため残課題。本番化前に公式ドキュメントで確定すること。

### Phase 4: 全体校正 + 直前チェックリスト
- 内容: 全章の整合（重複・矛盾の解消）、付録C 直前チェックリスト（7 思想 + 各章「最後に覚える一文」集約）、出題比率の最新化。
- 完了条件: 全 `src/*.md` が相互参照リンク切れなし、`doc/03_実装カタログ.md` の src 一覧が実体と一致。

---

## 3. End-to-end 検証

- 各 `src/0*.md` に 8 項目（§N.1〜§N.8）の見出しが揃っていること。
- 章 → `doc/01`、付録 ↔ 各章の相対リンクが切れていないこと。
- 付録A（シグナル語）・付録B（比較表）が本編の内容と矛盾しないこと。
- `make lint`（markdownlint 等）が通ること。※現状 Makefile はプレースホルダ。
- 揮発情報（出題比率・生成AIサービス名）に `⚠ 要公式確認` が付いていること。

---

## 4. 工数サマリ

| Phase | 内容 | 状態 |
|---|---|---|
| 0 | 初期化 | ✅ 完了 |
| 1 | 序章 + 最重点 4 章 + 付録A/B + 索引 | ✅ 完了 |
| 2 | 運用・データ管理（4 章: 05〜08） | ✅ 完了 |
| 3 | 生成AI + 横断（3 章: 09〜11） | ✅ 完了（生成AI名称の公式確定は残課題） |
| 4 | 全体校正 + 付録C | 未着手 |

---

## 5. やらないこと（スコープ外）

`01_仕様と設計.md §3.2` の破綻条件と整合させ、以下は明示的に作らない。

- **学習/模試プラットフォーム・採点アプリ** — 本書はコンテンツ教材であり、アプリ化はスコープ外。
- **実装ハンズオン教材化** — PMLE はコーディングを直接評価しない。手順書化に寄せると合格直結度が落ちる（破綻条件 4）。
- **サービス説明への偏り** — 使い方・画面操作の網羅説明はしない。「選ぶ/選ばない/ひっかけ」を主にする（破綻条件 1）。
- **本文と模擬問題の分離** — 章末問題集ではなく、本文中に判断材料を埋め込む（破綻条件 2）。
- **生成AIの後付け** — 旧来 ML だけで構成しない。Phase 3 で必ず含める（破綻条件 3）。
- **英語版・動画化** — 日本語 Markdown が canonical（`doc/README.md §4`）。多言語・動画は当面対象外。
